(* Pure lambda caclulus: *)

term        = application
            | "\" IDENTIFIER "." term ;

application = atom application
            | ; (*Empty*) 

atom        = "(" term ")"
            | IDENTIFIER ;