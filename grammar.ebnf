program     = statement* 'EOF' ;

statement   = (definition | term) NEWLINE ;

definition  = "let" IDENTIFIER "=" term ;

term        = application
            | "\" IDENTIFIER "." term ;

application = atom application
            | ; (*Empty*) 

atom        = "(" term ")"
            | IDENTIFIER ;